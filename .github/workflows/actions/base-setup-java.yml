name: BaseSetup-Java

description: Setup java 17 with Maven Cache

runs:
  using: composite
  
  steps:
    - name: Setup JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: "17"
        distribution: "temurin"
        cache: "maven"
